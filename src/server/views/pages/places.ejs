<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
    <title>Home</title>
  </head>
  <body>
    <% include ../partials/nav %>
    <div class="container">
      <header class="homePage">
        <h3> Explore a perfect day in the city.</h3>
      </header>
      <div class="row">
          <div class="col s12 m6 l4">
            <% places.forEach((place)=>{ %>
              <%= place.name %>
            <% })%>
          </div>
      </div>
    </div>
  </body>
</html>
